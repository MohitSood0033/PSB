import React from 'react';

interface EmpDetails {
    userDetails : any;
}

const EmpDet: React.FC<EmpDetails> = ({userDetails}) => {

    return (
        <>
            
        </>
    );
}

export default EmpDet;
